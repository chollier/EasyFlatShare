<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<% if current_user %>
	<%= image_tag @profile_image %>

	<%= @friends_allready_on_app.to_s %>
	<% @friends_allready_on_app.each do |x| %>
		<%= image_tag(@graph.get_picture(x["id"]))  %>
	<% end %>
<% end %>